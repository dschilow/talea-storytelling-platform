{
  "$schema": "https://encore.dev/schemas/infra.schema.json",
  "sql_servers": [
    {
      "host": "$PGHOST",
      "databases": [
        {
          "name": "avatar",
          "encore_name": "avatar",
          "username": "$PGUSER",
          "password": "$PGPASSWORD"
        },
        {
          "name": "doku",
          "encore_name": "doku",
          "username": "$PGUSER",
          "password": "$PGPASSWORD"
        }
      ]
    }
  ]
}
