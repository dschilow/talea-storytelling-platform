{
  "$schema": "https://encore.dev/schemas/infra.schema.json",
  "sql_servers": [
    {
      "host": "$PGHOST",
      "databases": {
        "avatar": {
          "database_name": "railway",
          "username": "$PGUSER",
          "password": "$PGPASSWORD"
        },
        "doku": {
          "database_name": "railway",
          "username": "$PGUSER",
          "password": "$PGPASSWORD"
        }
      }
    }
  ]
}
