{
  "$schema": "https://encore.dev/schemas/infra.schema.json",
  "sql_servers": [
    {
      "host": "$PGHOST",
      "databases": {
        "avatar": {
          "database_name": "railway",
          "username": "$PGUSER",
          "password": "$PGPASSWORD"
        },
        "doku": {
          "database_name": "railway",
          "username": "$PGUSER",
          "password": "$PGPASSWORD"
        },
        "personality_tracking": {
          "database_name": "railway",
          "username": "$PGUSER",
          "password": "$PGPASSWORD"
        },
        "story": {
          "database_name": "railway",
          "username": "$PGUSER",
          "password": "$PGPASSWORD"
        },
        "user": {
          "database_name": "railway",
          "username": "$PGUSER",
          "password": "$PGPASSWORD"
        }
      }
    }
  ],
  "object_storage": [
    {
      "gcs": {
        "bucket_name": "avatales-ai-logs-railway"
      },
      "buckets": {
        "avatales-ai-logs": {}
      }
    }
  ],
  "secrets": {
    "ClerkSecretKey": "$CLERK_SECRET_KEY",
    "OpenAIKey": "$OPENAI_API_KEY",
    "RunwareApiKey": "$RUNWARE_API_KEY"
  }
}
