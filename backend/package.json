{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist",
    "start": "encore run --port=${PORT:-4000}",
    "dev": "encore run",
    "deploy": "railway up"
  },
  "dependencies": {
    "@clerk/backend": "^1.34.0",
    "encore.dev": "^1.50.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "typescript": "^5.9.2"
  }
}