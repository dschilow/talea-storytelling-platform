{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist",
    "start": "bun run node_modules/encore.dev/dist/cli.js run"
  },
  "dependencies": {
    "@clerk/backend": "^1.34.0",
    "encore.dev": "^1.50.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}