{
  "$schema": "https://encore.dev/schemas/infra.schema.json",
  "sql_servers": [
    {
      "host": "postgres.railway.internal:5432",
      "tls_config": {
        "disable_ca_validation": true
      },
      "databases": {
        "user": {
          "name": "railway",
          "username": "postgres",
          "password": {
            "$env": "PGPASSWORD"
          }
        },
        "avatar": {
          "name": "railway",
          "username": "postgres",
          "password": {
            "$env": "PGPASSWORD"
          }
        },
        "story": {
          "name": "railway",
          "username": "postgres",
          "password": {
            "$env": "PGPASSWORD"
          }
        },
        "doku": {
          "name": "railway",
          "username": "postgres",
          "password": {
            "$env": "PGPASSWORD"
          }
        },
        "personality_tracking": {
          "name": "railway",
          "username": "postgres",
          "password": {
            "$env": "PGPASSWORD"
          }
        },
        "log": {
          "name": "railway",
          "username": "postgres",
          "password": {
            "$env": "PGPASSWORD"
          }
        }
      }
    }
  ],
  "buckets": [],
  "secrets": {
    "ClerkSecretKey": {
      "$env": "CLERK_SECRET_KEY"
    },
    "OpenAIKey": {
      "$env": "OPENAI_API_KEY"
    },
    "RunwareApiKey": {
      "$env": "RUNWARE_API_KEY"
    },
    "MCPServerAPIKey": {
      "$env": "MCP_SERVER_API_KEY"
    }
  }
}
