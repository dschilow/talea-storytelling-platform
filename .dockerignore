# Use nixpacks instead of Dockerfile
# This file prevents Docker build errors
# Railway will use nixpacks.toml configuration instead

node_modules
.git
.encore
*.log
.env*
!.env.railway

# Don't ignore MCP services directories
!mcp-services/
!mcp-services/mcp-main/
!mcp-services/mcp-validator/
