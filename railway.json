{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs_20", "bun"]
        },
        "install": {
          "cmds": ["bun install"]
        },
        "build": {
          "cmds": [
            "cd backend && bun install && bun run build"
          ]
        }
      },
      "start": {
        "cmd": "cd backend && npx encore run --port=$PORT"
      }
    }
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}