; Pin to the public registry. Without this, a machine-level ~/.npmrc pointing at a
; corporate feed (e.g. Azure DevOps) shadows the Expo/React Native packages and
; every install fails to resolve. Mirrors the repo-root .npmrc.
registry=https://registry.npmjs.org/
