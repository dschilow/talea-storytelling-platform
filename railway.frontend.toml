# ⚠️ WARNUNG: Railway ignoriert diese Datei standardmäßig!
# Diese Datei ist nur zur REFERENZ/DOKUMENTATION.
# 
# 🔧 Du MUSST den Frontend Service manuell im Railway Dashboard konfigurieren:
# 
#   Frontend Service → Settings → Build → Dockerfile Path: Dockerfile.frontend
#
# ⚠️ PROBLEM: Das Feld ist schreibgeschützt?
#   → Siehe FRONTEND_DOCKERFILE_FIX.md für Lösungen!
#
# Railway liest nur EINE railway.toml pro Projekt (nicht pro Service).
# Solange railway.toml existiert, überschreibt sie ALLE Services!

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.frontend"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"

[[environmentVariables]]
name = "PORT"
value = "80"
